name: DoNothing Main

on:
  push:
    branches:    
      - main
    paths:
      - 'Frends.TestTask.DoNothing/**'
  workflow_dispatch:
  
jobs:
  linux_build:
    name: build on Ubuntu
    uses: FrendsPlatform/FrendsTasks/.github/workflows/build_main_partner.yml@fspw-320
    with:
      workdir: Frends.TestTask.DoNothing