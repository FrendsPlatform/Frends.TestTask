name: DoNothing Test

on:
  push:
    branches-ignore:
      - main
    paths:
      - 'Frends.TestTask.DoNothing/**'
  workflow_dispatch:
  

jobs:
  windows_build:
    name: build on Windows
    uses: FrendsPlatform/FrendsTasks/.github/workflows/build_test_partner.yml@fspw-320
    with:
      workdir: Frends.TestTask.DoNothing