name: DoNothing Release

on:
  workflow_dispatch:
  

jobs:
  build:
    uses: FrendsPlatform/FrendsTasks/.github/workflows/release.yml@fspw-213
    with:
      workdir: Frends.TestTask.DoNothing
    secrets:
      feed_api_key: ${{ secrets.TASKS_FEED_API_KEY }}
      